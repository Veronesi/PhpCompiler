B = 5;
# Leemos un numero
readint("Ingresar un numero: ", Elem);
readlist("Ingresar una lista: ", A);
A = cons(Elem, A); I =3;
while I<=10 {
    readint(Elem);
    A = cons(Elem, A);
    I=I+1;
};